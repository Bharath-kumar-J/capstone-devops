{
  "id": null,
  "uid": "advmd2d",
  "title": "Average Response Time",
  "tags": ["capstone"],
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "5s",
  "time": { "from": "now-6h", "to": "now" },
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Average Response Time",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "custom": { "drawStyle": "line", "lineInterpolation": "linear", "fillOpacity": 0, "showPoints": "auto" }
        },
        "overrides": []
      },
      "gridPos": { "h": 10, "w": 10, "x": 0, "y": 0 },
      "targets": [
        {
          "refId": "A",
          "expr": "sum by (endpoint) (rate(app_request_latency_seconds_sum[1m])) / sum by (endpoint) (rate(app_request_latency_seconds_count[1m]))",
          "legendFormat": "{{endpoint}}"
        }
      ]
    }
  ]
}
